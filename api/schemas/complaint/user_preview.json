{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "complaint/user_preview.json",
  "title": "UserPreview",
  "description": "A short preview of a user referenced by a complaint. Matches the OpenAPI component schema `UserPreview`. All non-id fields are nullable because the live backend emits `null` for empty values (for example `urlLogo` for users without an avatar).",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "id": {"type": "integer"},
    "firstName": {"type": ["string", "null"]},
    "lastName": {"type": ["string", "null"]},
    "urlLogo": {"type": ["string", "null"]}
  }
}